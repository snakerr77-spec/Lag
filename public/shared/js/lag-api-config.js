window.LAG_API_CONFIG = Object.freeze({
  mode: "cloudflare",
  baseUrl: "",
  candidatesEndpoint: "/api/candidatosMedicos",
  maxResumeBytes: 10 * 1024 * 1024,
  supportEnabled: true,
  supportTicketsEndpoint: "/api/support/tickets"
});
